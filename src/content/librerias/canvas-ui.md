---
nombre: "Canvas UI"
categoria: "components"
claim: 
  en: "Canvas and WebGL effects that live in your repo."
  es: "Efectos de canvas y WebGL que viven en tu repo."
descripcion: 
  en: "An open-source collection of html-in-canvas and WebGL effects — particle reveals, liquid, glass, shatter, VHS — that wrap your real content instead of replacing it. There is no package to install: the shadcn CLI pulls the source into your project, so the code is yours from day one and nothing updates from under you. Every effect ships in six flavors — React, Solid, Preact, Vue, Svelte and dependency-free vanilla TypeScript — same engine, same props."
  es: "Una colección de código abierto de efectos de html-en-canvas y WebGL —revelados por partículas, líquido, cristal, astillado, VHS— que envuelven tu contenido real en vez de reemplazarlo. No hay paquete que instalar: la CLI de shadcn se trae el código a tu proyecto, así que es tuyo desde el primer día y nada se actualiza por debajo. Cada efecto se publica en seis sabores —React, Solid, Preact, Vue, Svelte y TypeScript vanilla sin dependencias— con el mismo motor y las mismas props."
nota:
  en: "The components that draw live HTML on canvas lean on a browser capability that today only Chrome has, behind a flag. Everywhere else they degrade instead of breaking: your content renders as regular HTML and the effect keeps running as a WebGL layer on top. The purely WebGL ones work in every modern browser. The license is MIT plus Commons Clause: free in any project, personal or commercial, but the components themselves cannot be resold or redistributed."
  es: "Los componentes que dibujan HTML vivo sobre canvas se apoyan en una capacidad del navegador que hoy solo tiene Chrome, y detrás de un flag. En el resto degradan en vez de romperse: tu contenido se pinta como HTML normal y el efecto sigue corriendo como una capa de WebGL encima. Los que son solo WebGL funcionan en cualquier navegador moderno. La licencia es MIT más Commons Clause: gratis en cualquier proyecto, personal o comercial, pero los componentes en sí no se pueden revender ni redistribuir."
url: "https://canvasui.dev/"
imagen: "../../assets/canvas-ui.webp"
orden: 30
---
